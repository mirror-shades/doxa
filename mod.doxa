import "./safeMath.doxa" as math; 

var x is math.safeAdd(30, 4); 

x?;





```x
import "./safeMath.doxa" as math; 

var x is math.safeAdd(30, 4); 
```


```
import "./math.doxa" as math; 

pub function safeAdd(, b) {
    if(a > 255 or b > 255) then {
        print "Overflow";
        return -1;
    } else {
        return math.add(a, b);
    }
}
```
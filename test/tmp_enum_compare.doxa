enum Color {
    Red,
    Green,
}

enum Stoplight {
    Red,
    Yellow,
}

public entry function main() {
    const a is Color.Red
    const b is Stoplight.Red
    const c is Color.Green

    (a == b)?
    (a == c)?
}

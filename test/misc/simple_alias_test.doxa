// Simple test for the new alias system
function testSimpleAlias(^x :: int) {
    x += 1
    return x
}

function main() {
    var y is 5
    var result is testSimpleAlias(^y)
    @print("y: {y}\n")
    @print("result: {result}\n")
}

var hello is "hello"
var arr is [1, 2, 3, 4, 5]
var emptyArr is []
var emptyStr is ""
var unicode_str is "café"


/////
// LENGTH
/////

@length("hello")?
@length(hello)?
@length([1, 2, 3, 4, 5])?
@length(arr)?
@length("")?
@length([])?
@length([42])?


/////
// PUSH
/////

@push(arr, 6)?
@push(hello, "!")?

@push([1, 2, 3, 4], 5)?
@push("hello", "!")?

hello?
arr?

@push(hello, "")?
@push(emptyArr, 1)?
@push(emptyStr, "!")?

hello?
emptyArr?
emptyStr?


/////
// POP
/////

@pop(arr)?
@pop(hello)?

arr?
hello?

@pop(emptyArr)?
@pop(emptyStr)?

emptyArr?
emptyStr?

@pop(unicode_str)?

"end"?
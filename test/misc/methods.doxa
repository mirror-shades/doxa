var hello is "hello"
var arr is [1, 2, 3, 4, 5]
var nested is [[1, 2], [3, 4], [5, 6]]
var emptyArr is []
var emptyStr is ""
var unicode_str is "café"


/////
// LENGTH
/////

@length("hello")?
@length(hello)?
@length([1, 2, 3, 4, 5])?
@length(arr)?
@length("")?
@length([])?
@length([42])?


/////
// PUSH
/////

@push(arr, 6)?
@push(hello, "!")?

@push([1, 2, 3, 4], 5)?
@push("hello", "!")?

hello?
arr?

@push(hello, "")?
@push(emptyArr, 1)?
@push(emptyStr, "!")?

hello?
emptyArr?
emptyStr?


/////
// POP
/////

@pop(arr)?
@pop(hello)?

arr?
hello?

@pop(emptyArr)?
@pop(emptyStr)?

emptyArr?
emptyStr?

@pop(unicode_str)?
unicode_str?

@pop(nested)?
nested?

/////
// CASTING
/////

@int("123")?
@int("123.4")?
@int("0x0A")?
@int("hello")?
@int(123.4)?
@int(0x0A)?
@int(0)?

@float("123")?
@float("123.4")?
@float("0x0A")?
@float("hello")?
@float(123)?
@float(0x0A)?
@float(0)?

@byte("123")?
@byte("123.4")?
@byte("0x0A")?
@byte("hello")?
@byte("a")?
@byte(123.4)?
@byte(0x0A)?
@byte(0)?






"end"?
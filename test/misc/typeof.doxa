var _int = 5;
typeof(_int)?;

var _float = 5.5;
typeof(_float)?;

var _string = "hello";
typeof(_string)?;

var _boolean = true;
typeof(_boolean)?;

var _array = [1, 2, 3];
typeof(_array)?;

struct Point {
    x: int,
    y: int,
}

typeof(Point)?;

var p = Point {
    x = 1,
    y = 2,
};

typeof(p)?;

enum Color {
    Red,
    Green,
    Blue,
}

typeof(Color)?;
typeof(Color.Red)?;

function fib(n :: int) returns(int) {
    if n <= 1 then return n;
    return fib(n - 1) + fib(n - 2);
}

fib(0)?; // Should be 0
fib(1)?; // Should be 1  
fib(2)?; // Should be 1
fib(3)?; // Should be 2 
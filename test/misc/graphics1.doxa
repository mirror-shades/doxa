module g from "graphics"

entry function main() {
    g.raylib.InitWindow(800, 600, "Doxa + Raylib")
    while not g.raylib.WindowShouldClose() {
        g.raylib.BeginDrawing()
        g.raylib.ClearBackground(g.raylib.SKYBLUE)
        g.raylib.EndDrawing()
    }
    g.raylib.CloseWindow()
}
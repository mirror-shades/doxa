entry function main() {
    const input is @read("./advent.txt")
    var new_count is 50
    var previous_count is 50
    var zeroes_hit :: int
    
    for c while c < @length(input) do c += 1 {
        if input[c] == "L" or input[c] == "R" then {
            var mod is if input[c] == "R" then 1 else -1
            var s_num :: string
            c += 1
            while input[c] != "\n" do c += 1 {
                s_num += input[c]
            }
            const num is @int(s_num)
            previous_count is new_count
            new_count += num * mod
            var _count is previous_count
            if new_count > previous_count then {
                _count += 1
                while _count <= new_count do _count += 1 {
                    if(_count % 100 == 0) then {
                        zeroes_hit += 1
                    }
                }
            } else {
                _count -= 1
                while _count >= new_count do _count -= 1 {
                    if(_count % 100 == 0) then {
                        zeroes_hit += 1
                    }
                }
            }
        }
    } 
}
module g from "graphics"

entry function main() {
    const rl is g.raylib
    rl.InitWindow(800, 600, "Doxa + Raylib")
    while not rl.WindowShouldClose() {
        rl.BeginDrawing()
        rl.ClearBackground(rl.GREEN)
        rl.EndDrawing()
    }
    rl.CloseWindow()
}

// -> is used for return types in other languages
// this is more intuitive and consistent and less verbose than returns(...)

// current returns syntax
fn something() returns(int | float) {...}

// proposed returns syntax
// possible to use the -> symbol for returns
fn something() -> int | float {...}

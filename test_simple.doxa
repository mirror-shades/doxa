var test_var is "hello";
test_var is "world";
test_var?;

// Global variable - not public
var global_num is 42;

// Simple test for debugging import issues  
pub function simple() returns(int) {
    return global_num;  // This should fail when called from another module
}

pub function getGlobal() returns(int) {
    return global_num;  // This will also fail
} 